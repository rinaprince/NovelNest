{% extends 'base.html.twig' %}

{% block title %}Hello LoginController!{% endblock %}

{% block body %}
    <div class="container d-flex justify-content-center py-5">
        <div class="row">
            <div class="col-12">
                <h2 class="fs-3 pt-5 pb-2">Inicio de sesión</h2>
            </div>
        </div>
    </div>

    <div class="container-fluid d-flex justify-content-center">
        <div class="row">
            <div class="col-12">
                {% if error %}
                    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}

                <form action="{{ path('app_login') }}" method="post">
                    <label for="username" class="mb-4"><strong>Nom de usuario:</strong></label>
                    <input class="form-control" type="text" id="username" name="_username" value="{{ last_username }}"><br>

                    <label for="password" class="mb-4"><strong>Contraseña:</strong></label>
                    <input class="form-control" type="password" id="password" name="_password"> <br>

                    <button type="submit" class="btn btn-outline-info">Entrar</button>
                </form>

                <a href="#">¿No tienes una cuenta? ¡Regístrate aquí!</a>
            </div>
        </div>
    </div>
{% endblock %}
