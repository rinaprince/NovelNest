<form method="post" action="{{ path('app_obra_delete', {'id': obra.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ obra.id) }}">
    <button class="btn">Delete</button>
</form>
